rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access keeps gallery/review media visible on the website.
    // Restrict writes to authenticated admins only.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
